Good — Node 1 is up. Before I give you the Node 2 files, I have to flag one real bug in your Node 2 config that the others didn't have, because it will break memberlist if left as-is.

## Bug specific to Node 2 — double colon in `join_members`

In Node 2's current `loki-config.yaml`:
```yaml
- "${NODE2_IP}:${MEMBERLIST_PORT_DISTRIBUTOR_SECONDARY_1}" # 10.189.35.11::7954
```
The comment shows `10.189.35.11::7954` — a **double colon**. If that's just a comment typo it's harmless, but verify the actual rendered value isn't `10.189.35.11::7954` (which would be an invalid gossip address). The corrected file below uses the env vars cleanly so it renders as `10.189.35.11:7954`.

Now applying the same three fixes (rate strategy, env-driven rate, 4 MB line size, reject_old_samples off) plus the broadened seeds, adapted to Node 2's variable names.Both Node 2 files are ready. Rename them when you drop them in: `node2-loki-config.yaml` → `loki-config.yaml` and `node2.env` → `.env`.

## What changed for Node 2

Same three core fixes as Node 1, adapted to Node 2's variable names:
- `ingestion_rate_strategy: local` added to `limits_config` — the actual 429 fix.
- `INGESTION_RATE_MB` 64 → 256, `INGESTION_BURST_SIZE_MB` 128 → 512.
- `MAX_LINE_SIZE` 1 MB → 4 MB, and `reject_old_samples: true` → `false`.

Node-2-specific values are preserved exactly: `INSTANCE_IP=10.189.35.11`, `MEMBERLIST_PORT=7954` base, `REDIS_PORT=6380`, and the secondary port blocks. I also cleaned the `join_members` (the original comment showed a `10.189.35.11::7954` double-colon) and removed the duplicate `MAX_ENTRIES_LIMIT_PER_QUERY` (was 5000 then 20000 — kept 20000).

## One thing I could NOT fix in these files — your `docker-compose.yml`

Node 2's compose file has the **same `S3_INSECURE= true` bug on both distributor blocks** (`distributor-secondary-1` and `distributor-secondary-2`). As discussed, it's cosmetic (distributors don't write to S3), so it won't block the 429 fix — but if you want consistency, hand-edit both lines to `S3_INSECURE=${S3_INSECURE}` before bringing Node 2 up.

## Apply on Node 2

```bash
cd /root/dclogforward1/multi-node/node2
cp loki-config.yaml loki-config.yaml.bak && cp .env .env.bak
# drop in the new files (renamed), then recreate so .env is re-read:
docker compose down && docker compose --profile redis up -d
```

## Verify the whole cluster

```bash
sleep 30
# Ring should show 4 ACTIVE ingesters again (2 per node)
curl -s http://10.189.35.10:3100/ring | grep -c ACTIVE       # expect 4
curl -s http://10.189.35.11:3100/ring | grep -c ACTIVE       # expect 4

# No new rate-limit rejections on either node's distributors
docker logs loki-distributor-secondary-1-node2 2>&1 | grep -i "rate limit" | tail -5
docker logs loki-distributor-primary-1-node1   2>&1 | grep -i "rate limit" | tail -5

# Grafana side clears (429 → 204)
oc -n openshift-logging logs -f <collector-grafana-pod> | grep -E "429|400|204"
```

Once both nodes are recreated, all four distributors enforce 256 MB/s locally instead of the old 32 MB/s, and the `collector-grafana` 429s should stop — logs will start flowing into Grafana.

Since you've now confirmed Node 1 containers are all up and healthy, you're safe to recreate Node 2; the cluster keeps serving on Node 1's ingesters during the brief Node 2 restart.

And the standing reminder: those S3 access/secret keys are live in both `.env` files — worth rotating the NetApp credentials when you get a maintenance window.
