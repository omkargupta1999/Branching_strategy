Good — since you've already deployed to the cluster, let's verify directly there instead of downloading locally. This gives you both the file-level confirmation and the real runtime test in one pass.

## Step 1 — Confirm the jar contents inside the running pod

First get the pod name and exec in:

```bash
oc get pods -n dev-transaction | grep paymentservice
```

```bash
oc exec -it <pod-name> -n dev-transaction -- sh
```

Once inside, find the actual running jar (you already know the pattern from before — `epay_payment_service-dev-*.jar` sitting in the container's working directory):

```bash
ls -la / | grep epay_payment_service
```

Then check what's in `BOOT-INF/lib` using `jar` (confirmed present in this image from your last interaction — `which jar` worked, `unzip` did not):

```bash
jar tf /epay_payment_service-dev-*.jar | grep "BOOT-INF/lib/" | grep -i "SBINEWPG\|jackson"
```

**What you want to see:**
- `BOOT-INF/lib/SBINEWPG_2023-cleaned.jar` → present
- `BOOT-INF/lib/SBINEWPG_2023.jar` → **absent** (no unsuffixed original)
- Every `jackson-*.jar` line → version `2.17.2`

## Step 2 — Confirm the cleaned jar has zero embedded Jackson classes

Still inside the pod:

```bash
cd /tmp
jar xf /epay_payment_service-dev-*.jar BOOT-INF/lib/SBINEWPG_2023-cleaned.jar
jar tf BOOT-INF/lib/SBINEWPG_2023-cleaned.jar | grep -i jackson
```

This should return **nothing**. Any output here means the strip didn't fully work and we need to look at the `exclude` pattern again.

Exit the pod once done:

```bash
exit
```

## Step 3 — The real test: hit the endpoint that used to crash

From your bastion, outside the pod, trigger whatever request previously caused the `NoSuchFieldError` — based on your earlier logs, that was the `/api/payments/v1/inb/sbi/initiate` endpoint, and also the `CashChallanStatusQueryListener` Kafka consumer path. You'll need to trigger this the same way QA/your test harness normally does (Postman, a test script, or however that endpoint is normally invoked in `dev-transaction`).

While that request is in flight or just after, watch the pod logs live:

```bash
oc logs -f <pod-name> -n dev-transaction | grep -i jackson
```

**No output at all** = no `NoSuchFieldError` thrown = fixed.

Also worth a broader error check in case anything else surfaces:

```bash
oc logs -f <pod-name> -n dev-transaction | grep -i error
```

## Step 4 — Confirm the pod itself is stable

Since it was crash-looping before, check restart count has stopped climbing:

```bash
oc get pods -n dev-transaction | grep paymentservice
```

Look at the `RESTARTS` column — if it stays flat after a few minutes of traffic (instead of incrementing), that's strong independent confirmation alongside the log check.

---

Run Step 1 and Step 2 first and paste me the output — that confirms the jar itself is clean before you spend time triggering the actual payment flow in Step 3.
