


Just **one line change** needed in your `.dockerbuild` stage — add `OPENJDK_IMAGE_TAG` to the existing `podman build` command:

---

## Updated `.dockerbuild` Stage

```yaml
.dockerbuild:
  stage: dockerbuild
  script:
    - ls -ltrh
    - echo $VERSION
    - TAG="${VERSION}"
    - echo "Generated Tag.. ${TAG}"
    - podman login -u $IMAGE_REGISTRY_USERNAME -p $IMAGE_REGISTRY_PASS $CI_TEMPLATE_REGISTRY_HOST --tls-verify=false
    - podman build -t $CI_TEMPLATE_REGISTRY_HOST/$IMAGE_NAME:$TAG -f Dockerfile --tls-verify=false --build-arg ENV=$ENV --build-arg VERSION=$TAG --build-arg OPENJDK_IMAGE_TAG=$OPENJDK_IMAGE_TAG
    - podman push $CI_TEMPLATE_REGISTRY_HOST/$IMAGE_NAME:$TAG --tls-verify=false
```

---

## What Changed

```bash
# Before
--build-arg ENV=$ENV --build-arg VERSION=$TAG

# After
--build-arg ENV=$ENV --build-arg VERSION=$TAG --build-arg OPENJDK_IMAGE_TAG=$OPENJDK_IMAGE_TAG
```

Only `--build-arg OPENJDK_IMAGE_TAG=$OPENJDK_IMAGE_TAG` was appended — everything else remains untouched.

---

## Updated Dockerfile

```dockerfile
ARG OPENJDK_IMAGE_TAG=1.22-1.1749462973
FROM registry.dev.sbiepay.sbi:8443/ubi9/openjdk-21:${OPENJDK_IMAGE_TAG}

ARG ENV
ARG VERSION
ENV ENV=$ENV
ENV VERSION=$VERSION

COPY /build/libs/epay_admin_service-${VERSION}.jar .
ENTRYPOINT sh -c 'java -jar "epay_admin_service-$VERSION.jar"'
```

---

## Complete Picture

| Component | Change |
|---|---|
| `.dockerbuild` stage | Add `--build-arg OPENJDK_IMAGE_TAG=$OPENJDK_IMAGE_TAG` to `podman build` |
| `Dockerfile` | Add `ARG OPENJDK_IMAGE_TAG` before `FROM` line |
| GitLab Variable | Already created ✅ |
| `.build` (Gradle) stage | No change needed ✅ |


















==============================================================

====below is our current build configuration....
 
 
 
.dockerbuild:
  stage: dockerbuild
  script:
    - ls -ltrh
    - echo $VERSION
    - TAG="${VERSION}"
    - echo "Generated Tag.. ${TAG}"
    - podman login -u $IMAGE_REGISTRY_USERNAME -p $IMAGE_REGISTRY_PASS $CI_TEMPLATE_REGISTRY_HOST --tls-verify=false
    - podman build -t $CI_TEMPLATE_REGISTRY_HOST/$IMAGE_NAME:$TAG -f Dockerfile --tls-verify=false --build-arg ENV=$ENV --build-arg VERSION=$TAG
    - podman push $CI_TEMPLATE_REGISTRY_HOST/$IMAGE_NAME:$TAG --tls-verify=false 
	
	
We have created a gitlab variavle name : OPENJDK_IMAGE_TAG and sharing below dockerfile for your reference ..........

Please let me know the what changes are required in build here configuration share the updated one....

























Dockerfile to build the application build  ..............

FROM registry.dev.sbiepay.sbi:8443/ubi9/openjdk-21:1.22-1.1749462973
ARG ENV
ARG VERSION

ENV ENV=$ENV
ENV VERSION=$VERSION

COPY /build/libs/epay_admin_service-${VERSION}.jar .
# ENTRYPOINT ["java","-jar","epay_admin_service-$VERSION.jar"]
ENTRYPOINT sh -c 'java -jar "epay_admin_service-$VERSION.jar"'


We have multiple application services and have multiple dockerfile and each dockerfile we are using the same openjdk-21 image.. so whenever we have upgrade the jdk image we have to update every dockerfile from each dockerfile in all the branches...

So we want something to make it dynamic and we will pass a variable like openjdk-21-version: 1.22-1.1749462973 in our Gitlab and it should take it from the variable that value and update it while building teh images in pipeline....i am also sharing the build stage configuration to you ....please check and suggest the best possible solution....

and like this we can pass the 


FROM registry.dev.sbiepay.sbi:8443/ubi9/openjdk-21:${openjdk-21-version}


java.gradle.yml -------------

.build:
  stage: build
  image: artifactory.jfrog.sbi:443/itepaypg-sbiepay2-docker-local/custom-ci/gradle-9.3.0-jdk-21:1.24-2.1776357031
  tags:
    - common_runner
  variables:
    GRADLE_OPTS: "-Dorg.gradle.daemon=false"
  script:
    - |
      if [ -n "$VERSION" ]; then
        gradle build -x test -Pversion=${VERSION}
      else
        gradle build -x test
      fi
    - ls -lrth build/libs
    - echo "VERSION=$VERSION" >> build.env
  artifacts:
    paths:
      - build/libs
    reports:
      dotenv: build.env
    when: on_success
    expire_in: 1 week
